Makefile Explanatory Document
=============================

Makefile will compile code for the scheme-list lab. 

The clean option will remove all compiled files, otherwise known as .o files, from the directory. 